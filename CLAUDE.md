# コミュニケーション
- 簡潔に回答し、自明な説明は省略する
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

# コードスタイル
- 関数型アプローチを優先し、副作用を最小化する
- 厳密な型付け（anyは使わずunknownを使う）
- エラーは握りつぶさない
- 必ずテストコードを作成する

# Git規約
- Conventional Commits形式、本文は日本語（例: `feat: ユーザー認証にOAuth2を追加`）
- 確認なしに自動コミット・自動pushしない

# 禁止事項
- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない


# エージェント型SDLCとSpec駆動開発

KiroスタイルのSpec駆動開発をエージェント型SDLCで実践する

## プロジェクトコンテキスト

### パス
- ステアリング: `.kiro/steering/`
- スペック: `.kiro/specs/`

### ステアリングとスペックの違い

**ステアリング** (`.kiro/steering/`) — プロジェクト全体のルールとコンテキストでAIを誘導する
**スペック** (`.kiro/specs/`) — 個別機能の開発プロセスを形式化する

### アクティブなスペック
- `.kiro/specs/` でアクティブなスペックを確認する
- `/kiro-spec-status [機能名]` で進捗を確認する

## 開発ガイドライン
- 思考は英語で行い、応答は日本語で生成する。プロジェクトファイル（例: requirements.md, design.md, tasks.md, research.md, バリデーションレポート）に書き込むMarkdownコンテンツは、そのスペックで設定された対象言語（spec.json.language を参照）で記述すること。

## 最小ワークフロー
- フェーズ0（任意）: `/kiro-steering`、`/kiro-steering-custom`
- 発見: `/kiro-discovery "アイデア"` — アクションパスを決定し、マルチスペックプロジェクト用に brief.md と roadmap.md を作成する
- フェーズ1（スペック作成）:
  - 単一スペック: `/kiro-spec-quick {機能} [--auto]` またはステップごとに:
    - `/kiro-spec-init "説明"`
    - `/kiro-spec-requirements {機能}`
    - `/kiro-validate-gap {機能}`（任意: 既存コードベース向け）
    - `/kiro-spec-design {機能} [-y]`
    - `/kiro-validate-design {機能}`（任意: デザインレビュー）
    - `/kiro-spec-tasks {機能} [-y]`
  - マルチスペック: `/kiro-spec-batch` — roadmap.md から依存関係の波ごとに並列でスペックを全作成する
- フェーズ2（実装）: `/kiro-impl {機能} [タスク番号]`
  - タスク番号なし: 自律モード（タスクごとにサブエージェント＋独立レビュー＋最終バリデーション）
  - タスク番号あり: 手動モード（選択タスクをメインコンテキストで実行、完了前にレビュアーゲートあり）
  - `/kiro-validate-impl {機能}`（単独の再バリデーション）
- 進捗確認: `/kiro-spec-status {機能}`（いつでも使用可能）

## スキル構成
スキルは `.claude/skills/kiro-*/SKILL.md` に配置される
- 各スキルは `SKILL.md` ファイルを含むディレクトリ
- スキルは会話コンテキストにアクセスしながらインラインで実行される
- スキルは効率化のためにサブエージェントへ並列調査を委譲できる
- スキルディレクトリにはテンプレートや例などの追加ファイルを加えられる
- `kiro-review` — レビュアーサブエージェントが使用するタスクローカルの敵対的レビュープロトコル
- `kiro-debug` — デバッガーサブエージェントが使用する根本原因優先デバッグプロトコル
- `kiro-verify-completion` — 成功・完了を主張する前の新鮮な証拠によるゲート
- **スキルが現在のタスクに1%でも適用される可能性があれば、必ず呼び出すこと。** タスクが簡単に見えてもスキルをスキップしないこと。

## 開発ルール
- 3フェーズ承認ワークフロー: 要件 → デザイン → タスク → 実装
- 各フェーズでヒューマンレビューが必要。意図的な高速化の場合のみ `-y` を使用する
- ステアリングを常に最新に保ち、`/kiro-spec-status` でアライメントを確認する
- ユーザーの指示に正確に従い、その範囲内で自律的に行動する: 必要なコンテキストを収集し、このセッションで作業をエンドツーエンドで完了させること。質問するのは、必須情報が欠けているか、指示が致命的に曖昧な場合のみ。

## ステアリング設定
- `.kiro/steering/` 全体をプロジェクトメモリとしてロードする
- デフォルトファイル: `product.md`、`tech.md`、`structure.md`
- カスタムファイルに対応（`/kiro-steering-custom` で管理）
